language: generic

sudo: required
services:
  - docker

script:
  - docker run --rm -v $TRAVIS_BUILD_DIR:/build -w=/build/src/Avast.SmsConnectorClient microsoft/aspnet:1.0.0-rc1-update1 dnu restore && dnu build && dnu pack
